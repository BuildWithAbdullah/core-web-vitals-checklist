{
  "name": "core-web-vitals-checklist",
  "version": "1.0.0",
  "private": true,
  "description": "Diagnosis order and fix order for LCP, INP and CLS, with fail and pass examples checked in CI.",
  "license": "MIT",
  "type": "module",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "test": "node --test \"test/**/*.test.mjs\"",
    "verify": "node tools/verify.mjs",
    "check": "node --check measure/attribution.mjs"
  }
}
